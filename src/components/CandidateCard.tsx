// will build a card for viewing a candidate 
// function that takes in an object or array of a user and displays that info 

function CandidateCard() {

    // Write a function that intakes a potential employee's info as an object 
    // Display that info on screen in a way that looks like the provided mockup
    
    // This should include: 
        // Profile avatar 
        // Login 
        // Location
        // Email 
        // Company
        // Bio  

    return {
        
    }
}

export default CandidateCard;